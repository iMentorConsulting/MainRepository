[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh ${START_SCRIPT:-start.sh}"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[environments.production.variables]
NODE_ENV = "production"
HOSTNAME = "0.0.0.0"
